# HDR POSTBACK SYSTEM

## POST BODY FORMAT

  ```JSON
  HTTPS POST {
    "body": [
      {
        "type": "string",
        "data": [
          {
            "mac": "string",
            "rssi": "string",
            "raw": "string",
            "time": "string",
          }
        ]
      }
    ],
  }
  ```
